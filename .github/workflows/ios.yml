name: iOS starter workflow

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        uses: actions/checkout@v2
        run: echo "Hello world"
        shell: bash
    goodbye:
      steps:
    name: goodbye world
    run: echo "Goodbye"
    shell: bash
